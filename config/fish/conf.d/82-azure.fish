#!/usr/bin/env fish
# Azure CLI completion

# Azure CLI provides fish completions automatically when installed via Homebrew
# The completions are typically installed to the fish vendor completions directory